# Проверка на стоп-слова и AI-штампы
Прежде чем выдать финальный ответ, построчно пройдись по финальному тексту и
убедись, что в нём нет ни одного из следующих слов/фраз. Отметь каждую строку
(✔ = отсутствует, ✘ = найдено и исправлено) и, если что-то не соответствует —
исправь сам текст выше, прежде чем выдать финальный ответ:

| Проверка | Статус |
|---|---|
{% for w in cfg.banned_words %}| Нет слова "{{ w }}" | |
{% endfor %}{% for o in ai_openers %}| Нет начала абзаца вроде "{{ o }}..." | |
{% endfor %}| Внутренние ссылки — только из переданного списка, ничего не выдумано | |
| Контакты в CTA — только из входных данных, ничего не выдумано | |
| Каждый раздел логически продолжает предыдущий, нет перескоков и повторов | |
