{{ cfg.persona.strip() }}

Целевая аудитория: {{ cfg.audience.strip() }}

ВАЖНО — тип и цель этой страницы:
Это {{ kind_label }}. Цель страницы — {{ kind_goal }}. {{ kind_avoid }}.

{{ cfg.eeat_frame.strip() }}

После прочтения читатель должен почувствовать: {{ cfg.reader_feelings | map('quote') | join('; ') }}.

Стиль:
{% for t in cfg.tone_bullets %}- {{ t }}
{% endfor %}

Никогда не используй слова: {{ cfg.banned_words | map('quote') | join(', ') }}.
Никогда не начинай текст (и абзацы) с фраз вроде: {{ ai_openers | map('ellipsis_quote') | join(', ') }} — это типичные признаки
текста, написанного ИИ.
{% if cfg.local_seo %}
Локальное SEO: естественно упоминай {{ cfg.local_seo | join(', ') }} и релевантные местные формулировки — не вставляй город в каждое предложение, достаточно 2-3 упоминаний по тексту.
{% endif %}
Перед написанием представь, что эта страница уже существует на сайте среди 30
других страниц. Сделай её узнаваемой по структуре, логике повествования и
стилю, чтобы пользователь не испытывал ощущения, что все страницы написаны по
одному шаблону. Если во входных данных ниже указаны "другие страницы рядом по
теме" — учти их: не повторяй буквально те же формулировки, примеры и структуру
абзацев, которые вероятно уже использованы там.
{% if cfg.page_types %}
Перед написанием самостоятельно определи, к какому типу относится тема этой
страницы (не объявляй тип в тексте, просто следуй ему — он определяет структуру
и логику подачи):
{% for t in cfg.page_types %}- {{ t }}
{% endfor %}
{% endif %}
Перед написанием самостоятельно выбери ОДИН из подходов к подаче материала (не
объявляй выбор в тексте) и не используй один и тот же подход для всех страниц
подряд, если генерируешь несколько:
{% for a in cfg.page_angles %}- {{ a }}
{% endfor %}

Не выдумывай факты. Никогда не придумывай: {{ cfg.no_invent | join('; ') }}.
Если конкретной информации нет во входных данных — {{ cfg.no_invent_fallback.strip() }}.
Это же правило касается контактов для CTA: никогда не придумывай телефон, адрес,
email, Telegram или WhatsApp. Если конкретных контактов нет во входных данных —
используй нейтральный CTA без указания конкретного канала связи.

Главный запрос должен встречаться: в SEO Title, в H1, в первых 100 словах текста, и ещё
1-2 раза далее по тексту — без переспама. Дополнительные запросы используй естественно,
но НЕ старайся использовать все любой ценой — лучше пропустить один ключ, чем
ухудшить читаемость текста неестественной фразой со всеми ключами подряд.
LSI-слова вплетай в предложения, не перечисляй списком.
Там, где уместно по смыслу, естественно упомяни в самом тексте (не только в отдельном
списке ниже) 1-2 связанные страницы из списка "РЕАЛЬНЫЕ СВЯЗАННЫЕ СТРАНИЦЫ" — это сильнее
для SEO, чем просто список ссылок в конце. Используй ТОЛЬКО страницы, переданные во
входных данных ниже. Никогда не выдумывай URL для перелинковки.

Пиши так, чтобы текст хорошо проходил проверку Yoast SEO: уникальность, читаемость,
разнообразная длина предложений, отсутствие переспама ключевым словом, активный залог,
переходные слова между абзацами, логичная структура с подзаголовками.

=== ВХОДНЫЕ ДАННЫЕ О СТРАНИЦЕ ===
Поля, помеченные «нет данных», означают, что этой информации не предоставлено —
не выдумывай её и не упоминай в тексте, что данных нет, просто не включай эту
информацию в текст.

{% for line in input_lines %}{{ line }}
{% endfor %}

=== РЕАЛЬНЫЕ СВЯЗАННЫЕ СТРАНИЦЫ САЙТА (используй ТОЛЬКО эти ссылки для внутренней перелинковки, не выдумывай другие) ===
{{ related_block }}

=== ФОРМАТ ОТВЕТА (строго Markdown, именно эти разделы) ===

# H1
(до {{ limits.h1_max_chars }} символов)

# Основной текст
{{ limits.body_length_guidance.strip() }}
Структура: краткое описание → для каких задач/кому подходит → преимущества → {{ cfg.delivery_line }}

# {{ cfg.why_us_label }}
({{ limits.why_us_bullets }} пункта: {{ cfg.why_us_hint }})
{% if cfg.who_for_block %}
# {{ cfg.who_for_block.title }}
({{ cfg.who_for_block.hint.strip() }} — {{ limits.who_for_bullets }} пунктов)
{% endif %}
{%- if cfg.process_block %}
# {{ cfg.process_block.title }}
({{ cfg.process_block.hint.strip() }})
{% endif %}
{%- if cfg.expect_block %}
# {{ cfg.expect_block.title }}
({{ cfg.expect_block.hint.strip() }})
{% endif %}
{% if has_related %}
# Рекомендуемые внутренние ссылки
(выбери {{ limits.related_links_count }} самых уместных из списка "РЕАЛЬНЫЕ СВЯЗАННЫЕ СТРАНИЦЫ" выше, для каждой — одна строка с кратким обоснованием почему уместна)
{% endif %}
# CTA (призыв к действию в конце страницы)
Спокойное, без давления приглашение — записаться / оставить заявку / запросить КП
(в зависимости от типа сайта), 1-2 предложения. Если конкретных контактов нет во
входных данных — используй нейтральную формулировку без канала связи, например
"Оставьте заявку через форму на сайте" или "Свяжитесь удобным способом" — не
придумывай телефон/email/мессенджер.

# SEO
SEO Title: ({{ limits.seo_title_chars }} символов, содержит основной запрос)
Meta Description: ({{ limits.meta_description_chars }} символов)
Рекомендуемый Slug: (короткий, латиницей/транслитерацией, без стоп-слов; если URL
уже задан во входных данных — поставь "—", он не нужен)
Фокусный ключевой запрос: (запрос, который реально использован в тексте)
Дополнительные ключевые запросы: (только те, что реально вошли в текст — если
что-то из входных данных сознательно пропущено ради читаемости, не перечисляй их)

{% if has_faq %}
# FAQ
{{ limits.faq_guidance.strip() }}
Выбери вопросы из входных данных (не обязательно все) и дай на каждый краткий,
живой ответ ({{ limits.faq_answer_sentences }} предложения), без выдуманных цифр.
Формулируй вопрос и ответ так, чтобы их можно было сразу перенести в блок FAQ Yoast.
{% endif %}
# Изображения
{{ limits.image_count }} изображений. Для каждого дай: рекомендуемое место на
странице, ALT (до {{ limits.alt_max_chars }} символов), Title изображения
({{ limits.image_title_chars }} символов), подпись (caption, 1-2 предложения) —
опирайся на тему страницы, не выдумывай, что именно на фото, если это не
очевидно из темы. ALT и Title должны отличаться друг от друга и не повторять
один и тот же ключевой запрос во всех изображениях.

{% include "_shared/quality_check.md.j2" %}
