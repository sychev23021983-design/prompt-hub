{{ cfg.persona.strip() }}

Целевая аудитория: {{ cfg.audience.strip() }}

ВАЖНО — тип и цель этой страницы:
Это {{ kind_label }}. Цель страницы — {{ kind_goal }}. {{ kind_avoid }}.

{{ cfg.eeat_frame.strip() }}

После прочтения читатель должен почувствовать: {{ cfg.reader_feelings | map('quote') | join('; ') }}.

Стиль:
{% for t in cfg.tone_bullets %}- {{ t }}
{% endfor %}

Никогда не используй слова: {{ cfg.banned_words | map('quote') | join(', ') }}.
Никогда не начинай текст (и абзацы) с фраз вроде: {{ ai_openers | map('ellipsis_quote') | join(', ') }} — это типичные признаки
текста, написанного ИИ.
{% if cfg.local_seo %}
Локальное SEO: естественно упоминай {{ cfg.local_seo | join(', ') }} и релевантные местные формулировки — не вставляй город в каждое предложение, достаточно 2-3 упоминаний по тексту.
{% endif %}
Если эта страница — не единственная, которую ты пишешь: каждая страница должна заметно
отличаться от других по структуре, формулировкам, примеру начала и логике изложения —
не используй одинаковые шаблонные первые абзацы от страницы к странице.

Не выдумывай факты. Никогда не придумывай: {{ cfg.no_invent | join('; ') }}.
Если конкретной информации нет во входных данных — {{ cfg.no_invent_fallback.strip() }}.

Главный запрос должен встречаться: в SEO Title, в H1, в первых 100 словах текста, и ещё
1-2 раза далее по тексту — без переспама. Дополнительные запросы используй естественно.
LSI-слова вплетай в предложения, не перечисляй списком.
Там, где уместно по смыслу, естественно упомяни в самом тексте (не только в отдельном
списке ниже) 1-2 связанные страницы из списка "РЕАЛЬНЫЕ СВЯЗАННЫЕ СТРАНИЦЫ" — это сильнее
для SEO, чем просто список ссылок в конце.

Пиши так, чтобы текст хорошо проходил проверку Yoast SEO: уникальность, читаемость,
разнообразная длина предложений, отсутствие переспама ключевым словом, активный залог,
переходные слова между абзацами, логичная структура с подзаголовками.

=== ВХОДНЫЕ ДАННЫЕ О СТРАНИЦЕ ===
{% for line in input_lines %}{{ line }}
{% endfor %}

=== РЕАЛЬНЫЕ СВЯЗАННЫЕ СТРАНИЦЫ САЙТА (используй ТОЛЬКО эти ссылки для внутренней перелинковки, не выдумывай другие) ===
{{ related_block }}

=== ФОРМАТ ОТВЕТА (строго Markdown, именно эти разделы) ===

# SEO Title
(финальный вариант, ≤{{ limits.seo_title_max_chars }} символов)

# H1

# Meta Description
({{ limits.meta_description_chars }} символов)

# Основной текст
({{ limits.body_word_count }} слов. Структура: краткое описание → для каких задач/кому подходит → преимущества →
{{ cfg.delivery_line }})

# {{ cfg.why_us_label }}
({{ limits.why_us_bullets }} пункта: {{ cfg.why_us_hint }})
{% if cfg.who_for_block %}
# {{ cfg.who_for_block.title }}
({{ cfg.who_for_block.hint.strip() }} — {{ limits.who_for_bullets }} пунктов)
{% endif %}
{%- if cfg.process_block %}
# {{ cfg.process_block.title }}
({{ cfg.process_block.hint.strip() }})
{% endif %}
{%- if cfg.expect_block %}
# {{ cfg.expect_block.title }}
({{ cfg.expect_block.hint.strip() }})
{% endif %}
# Рекомендуемые внутренние ссылки
(выбери {{ limits.related_links_count }} самых уместных из списка "РЕАЛЬНЫЕ СВЯЗАННЫЕ СТРАНИЦЫ" выше, для каждой — одна
строка с кратким обоснованием почему уместна; если список пуст — следуй инструкции в нём)
{% if has_faq %}
# FAQ
Выбери {{ limits.faq_count }} самых релевантных вопросов из входных данных (не обязательно все) и дай на каждый краткий, живой ответ ({{ limits.faq_answer_sentences }} предложения), без выдуманных цифр.
{% endif %}
# CTA (призыв к действию в конце страницы)
Спокойное, без давления приглашение — записаться / оставить заявку / запросить КП
(в зависимости от типа сайта), 1-2 предложения.

# Schema
Тип: {{ cfg.schema_type }}
Заполни поля: name, description, {{ cfg.schema_extra | join(', ') }}
{% if has_faq %}Если выбраны FAQ-вопросы выше — добавь отдельно FAQPage schema с этими вопросами и ответами.{% endif %}

# Изображения
Для {{ limits.image_count }} предполагаемых изображений на странице дай: ALT, Title, подпись (caption) —
опирайся на тему страницы, не выдумывай, что именно на фото, если это не очевидно из темы.

# Open Graph
OG Title:
OG Description:

{% include "_shared/quality_check.md.j2" %}
