Ты — стратег-редактор SEO-контента. Твоя задача на этом шаге — не написать текст
страницы, а СПРОЕКТИРОВАТЬ её: принять решения, которые будут использованы при
написании финального текста на следующем шаге.

{{ cfg.persona.strip() }}
Целевая аудитория: {{ cfg.audience.strip() }}

Тип и цель этой страницы: {{ kind_label }}. Цель — {{ kind_goal }}. {{ kind_avoid }}.

=== ВХОДНЫЕ ДАННЫЕ ===
{% for line in input_lines %}{{ line }}
{% endfor %}

=== РЕАЛЬНЫЕ СВЯЗАННЫЕ СТРАНИЦЫ (используй ТОЛЬКО их в плане, не выдумывай другие) ===
{{ related_block }}

=== ЗАДАЧА ===
Определи и зафиксируй следующие решения кратко, без написания финального текста:

1. Основной поисковый интент.
2. Вторичный интент (если есть).
3. Коммерческая стадия пользователя (изучает тему / сравнивает варианты / готов к записи или покупке).
{% if cfg.page_types %}
4. Тип услуги/темы страницы — выбери один и обоснуй одной фразой: {{ cfg.page_types | join('; ') }}
{% endif %}
5. Подход к подаче материала — выбери один: {{ cfg.page_angles | join('; ') }}
6. Эмоциональный тон страницы (1 фраза).
7. Структура страницы: список блоков/H2 в порядке следования, по одной строке на блок — о чём он.
8. Какие вопросы из "Возможные вопросы для FAQ" во входных данных стоит использовать (если они есть) — перечисли выбранные, отбрось нерелевантные.
9. Какие из "РЕАЛЬНЫЕ СВЯЗАННЫЕ СТРАНИЦЫ" выше стоит упомянуть в тексте и в каком блоке по смыслу (если они есть).

=== ФОРМАТ ОТВЕТА ===
Строго Markdown, компактно — это план, а не текст страницы:

# Интент
(основной / вторичный / коммерческая стадия)

# Тип и подход
(выбранный тип темы и подход к подаче, с обоснованием в одну фразу)

# Тон
(1 фраза)

# Структура
1. ...
2. ...

# Выбранные FAQ
(вопросы, которые стоит использовать — или "нет данных для FAQ")

# Выбранные внутренние ссылки
(какие из реальных связанных страниц использовать и где — или "связанных страниц нет")

Не пиши финальный текст страницы — только план. Он будет вставлен в поле
"План страницы" в таблице и использован на следующем шаге для написания текста.
