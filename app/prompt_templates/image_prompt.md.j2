Ты помогаешь подобрать рекомендации для изображений SEO-страницы.

{{ cfg.persona.strip() }}

Страница: {{ row.name or "нет данных" }}
Главный запрос: {{ row.primary_keyword or "нет данных" }}
{% if breadcrumb %}Раздел структуры сайта: {{ breadcrumb }}
{% endif %}
{% if row.page_plan %}
План страницы (учти его — изображения должны соответствовать структуре и тону,
которые уже определены в плане):
{{ row.page_plan }}
{% endif %}

Задача: подготовь рекомендации для {{ limits.image_count }} изображений страницы.
Для каждого дай:
- рекомендуемое место на странице;
- предполагаемый сюжет (без выдумывания конкретного содержимого фото, если это
  не очевидно из темы);
- ALT (до {{ limits.alt_max_chars }} символов, естественный, описательный, без переспама);
- Title изображения ({{ limits.image_title_chars }} символов);
- подпись (caption, 1-2 предложения).

ALT и Title должны отличаться друг от друга и не повторять один и тот же
ключевой запрос во всех изображениях. Не используй одинаковые шаблоны формулировок.

Формат ответа: Markdown, отдельный подзаголовок на каждое изображение
(## Изображение 1, ## Изображение 2, ...).
